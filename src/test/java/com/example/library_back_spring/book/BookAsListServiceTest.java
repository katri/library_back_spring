package com.example.library_back_spring.book;

import com.example.library_back_spring.view.BookView;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class BookAsListServiceTest {
    private final BookService bookService = new BookService();

    @Test
    void createNewBookWithAllValues() {
//        BookView bookView = new BookView("Sipsik", 1980,"lastekirjandus", "Eno Raud");
//
//        BookAsList createdBookAsList = bookService.addBook(bookView);
//
//        assertEquals("Sipsik", createdBookAsList.getTitle());
//        assertEquals("Eno Raud", createdBookAsList.getAuthor());
//        assertEquals(1980, createdBookAsList.getYearPublished());
//        assertEquals("lastekirjandus", createdBookAsList.getGenre());
    }
}